openapi: "3.1.0"
info:
  title: Minimal API
  version: "1.0.0"
servers:
  - url: https://api.example.com
paths:
  /items:
    get:
      summary: List items
      operationId: listItems
      responses:
        "200":
          description: Successful response
